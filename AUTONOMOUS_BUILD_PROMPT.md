# プロンプト：「アウトプットがインプットになる」開発学習アプリの自走開発

あなたはこれから、以下の仕様に基づいて1つのWebアプリケーションを最初から最後まで自走で実装するソフトウェアエンジニアです。ユーザーへの確認は「本当にブロッキングな技術的判断が必要な場合」のみに限定し、それ以外は本ドキュメントに書かれた方針とあなた自身の設計判断で実装を進めてください。各フェーズの終わりには実際にブラウザで動作確認を行い、動くことを確認してから次のフェーズに進んでください。

---

## 1. プロダクトコンセプト

**「アウトプットがインプットになる」体験** — 自分が作りたいものを作る過程そのものを、最高の学習教材に変える開発支援ツール。

### 背景（解決したい課題）
- ターゲットユーザーは「AIを併用しながら独学で開発しているエンジニア」。生成AIに技術スタック（Next.js, Prisma, Docker等）を提案されても、それが何をしているのか役割が分からない。
- 最適な設計が分からないため、結局「自分が使ったことがある技術」に逃げてしまい遠回りな実装になりがち。
- 結果として「自分が作った（AIに作らせた）アウトプットを本質的に理解できていない」状態に陥っている。

### コア体験（3ステップ）

**Step 1: 企画書 × パーソナライズ技術選定**
- ユーザーが企画書（自然文）を入力する。
- AI（後述のAIProvider経由で呼び出す）が対話形式の質問（「やりたいこと」の深掘り）を行い、最適な技術スタックを提案する。
- 企画書内の「実現したいこと」のフレーズを色分けし、それがどの技術要素によって実現されるかをグラフで可視化する（フロント・バック・インフラの関係性が一目で分かる）。
- 企画書の色付きフレーズをクリック/ホバーすると対応する技術ノードがハイライトされ、双方向にリンクする。

**Step 2: コード生成**
- 確定した技術スタックに基づき、AIが実際に動くプロジェクト一式（Next.jsアプリ）のコードを生成する。
- 生成後、WebContainers上で実際に `npm install` / `npm run dev` を実行し、ブラウザ内でライブプレビューできる状態にする。

**Step 3: コード理解サポート（Progate風ブロック穴埋め学習）**
- 生成された各ファイルを開くと、正解コードが薄いゴースト文字（Progateのお手本表示のイメージ）でエディタに表示される。
- ただし**タイピングでなぞらせるのではなく**、意味のある単位（役割ラベル付きの「ブロック」）に分解された選択肢をパレットから選ぶ/ドラッグすることで空欄を埋めていく。各空欄には正解1つ＋もっともらしい誤答が数個ある。
- 空欄が正しく埋まるたびに、その場でWebContainer内のファイルが更新され、隣のライブプレビューに即座に反映される。「このブロックを変えたら画面のここが変わった」を体感させる。
- ブロックには役割ラベル（例：「状態管理」「イベントハンドラ」「APIフェッチ」「JSXの見た目」など）と色分けがあり、細かすぎない抽象度で提示する（初心者が触る気をなくさないレベルに抑える）。

---

## 2. 確定した技術スタックとアーキテクチャ方針

これは検討の結果ユーザーと合意済みの方針です。**変更しないでください。**

| 領域 | 選定 | 理由 |
|---|---|---|
| フレームワーク | Next.js 15 (App Router) + TypeScript | v1で最も厚くサポートする対象。WebContainersとの相性が良い |
| スタイリング | Tailwind CSS + shadcn/ui | 高速に一貫したUIを組める |
| コードエディタ | CodeMirror 6（カスタム拡張） | ゴーストテキスト描画・空欄Decoration・ブロックはめ込みUIを自前実装する土台として採用 |
| ライブ実行/プレビュー | `@webcontainer/api`（StackBlitz WebContainers） | ブラウザ内でNode.jsランタイムを起動し実際に `next dev` を動かせる唯一の実用的手段。**Node.jsエコシステム限定**という制約がある |
| 技術スタック可視化 | React Flow | 企画書⇔技術ノードの関係グラフ描画 |
| 状態管理 | Zustand | 軽量。バックエンドDBを持たないため十分 |
| AI呼び出し | **ローカルLLM（Ollama, `http://localhost:11434`）を`AIProvider`インターフェース経由で呼び出す**（Vercel AI SDKでストリーミング） | 対話・技術選定・コード生成・コードのブロック分解（チャンク化）の4用途で使用。**Anthropic APIは絶対に使用しないこと** |
| AIプロバイダーの抽象化 | v1実装は`OllamaAIProvider`のみ。ローカルLLMのためAPIキーは不要（設定画面ではOllamaのエンドポイントURLとモデル名のみ入力させる）。将来的に`GeminiAIProvider`へ差し替える計画があるため、`AIProvider`インターフェース（`chat()`, `generateCode()`, `chunkCode()`等）を挟んで呼び出し元がプロバイダー実装に依存しないようにすること | ユーザーの明示的な要望。今はローカルLLM、将来はGemini APIに切り替える前提 |
| 永続化 | **ユーザー認証・DBなし**。プロジェクト状態（企画書・選定した技術スタック・生成コード・進捗）はブラウザのIndexedDB（`idb-keyval`等）にのみ保存し、リロードしても続きから再開できる程度に留める | ユーザーの明示的な要望。v1はローカル完結 |
| デプロイ | Vercel | WebContainers利用のため `Cross-Origin-Embedder-Policy: require-corp` / `Cross-Origin-Opener-Policy: same-origin` ヘッダーをNext.js設定で必ず付与すること |

### 実行エンジンの抽象化
WebContainersはNode.js系にしか対応できないという技術的制約があるため、コード実行部分は `ExecutionProvider` インターフェースとして抽象化すること。v1の実装は `WebContainerExecutionProvider` の1実装のみでよいが、将来Python/Go等のサーバーサイドサンドボックス（例：Docker, E2B等）を追加する際にUI層・学習ロジック層を変更せずに済むようにする。

### AIプロバイダーの抽象化
同様に、AI呼び出し部分は `AIProvider` インターフェースとして抽象化すること。v1の実装は `OllamaAIProvider` の1実装のみでよいが、将来 `GeminiAIProvider` に差し替える計画があるため、呼び出し元（対話UI・コード生成・チャンク化処理）はこのインターフェースにのみ依存させ、具体的なプロバイダー実装を意識しない設計にする。**Anthropic APIおよびAnthropicのSDK（`@anthropic-ai/sdk`等）は本プロジェクトで一切使用しないこと。**

**ただし、上記2つの拡張ポイント以外の過剰な抽象化・将来対応は行わないこと。**

---

## 3. スコープ外（v1では絶対に実装しないこと）

- ユーザーアカウント・ログイン・認証機能
- サーバーサイドDB・永続化ストレージ
- 課金・サブスクリプション機能
- Node.js以外の言語のサンドボックス実行（Python/Go/Rust等）
- モバイルアプリ・モバイル最適化（レスポンシブの最低限対応のみでよい）
- Scratch的なフルビジュアルプログラミング（今回のブロックはめ込みは「穴埋め」であり、自由にブロックを組み合わせて任意のロジックを組み立てる機能ではない）
- マルチプレイヤー・共同編集機能
- **Anthropic API / Anthropic SDKの使用（Claude Code自身がこのドキュメントを実装するエージェントであることと、アプリ内で呼び出すAIモデルは無関係。アプリ内で呼び出すAIは必ずローカルLLM経由にすること）**
- `GeminiAIProvider`の実装（インターフェースの型定義・差し替え口を用意するところまでで良く、実際にGemini APIを呼ぶ実装はv1では不要）

これらの提案・実装は行わず、上記に該当する要求が出てもv1のスコープには含めないこと。

---

## 4. 実装フェーズ（この順序で進め、各フェーズ末尾で必ずブラウザ動作確認を行うこと）

### Phase 0: プロジェクト基盤
- Next.js 15 (App Router, TypeScript) プロジェクトを初期化
- Tailwind CSS, shadcn/ui, Zustand, `idb-keyval` を導入
- 全体のページ構成（ランディング／企画書入力／技術選定／コード理解の3〜4画面）のルーティング雛形を作成
- AIプロバイダー設定UIを実装（Ollamaのエンドポイント URL・モデル名の入力、疎通確認ボタン）。設定はlocalStorageに保存する
- **受け入れ基準**: `npm run dev` でアプリが起動し、各画面に遷移でき、Ollamaのエンドポイント設定を入力するとlocalStorageに保存され、疎通確認が成功することをブラウザで確認する（事前にユーザーのマシンで`ollama serve`とモデルのpullが済んでいることを前提にしてよい）

### Phase 1: 企画書 → 技術選定 → 可視化
- 企画書入力フォームを実装
- `AIProvider`インターフェースと`OllamaAIProvider`実装、およびNext.jsのRoute Handler経由でOllamaへリクエストを転送するプロキシ（ストリーミング対応）を実装
- AIとの対話形式（質問→回答→技術スタック提案）のチャットUIを実装
- 企画書テキストの色分けハイライト表示と、React Flowによる技術スタック関係グラフを実装
- 双方向リンク（企画書のフレーズをクリック→対応ノードをハイライト、逆も同様）を実装
- **受け入れ基準**: 実際にローカルのOllamaを起動した状態で企画書を入力し、対話を経て技術スタックが提案され、色分けとグラフのリンクが機能することをブラウザで確認する

### Phase 2: コード生成 + ライブプレビュー基盤
- 確定した技術スタックに基づき、AI（OllamaAIProvider経由）に実際のNext.jsプロジェクトのファイル一式を生成させる機能を実装
- `@webcontainer/api` を統合し、生成されたファイルをWebContainerにマウントして `npm install && npm run dev` を実行、iframeでライブプレビューを表示
- 必要なCOOP/COEPヘッダーをNext.js設定に追加
- **受け入れ基準**: コード生成後、実際にWebContainer上でアプリが起動しプレビューに表示されることをブラウザで確認する

### Phase 3: ブロック穴埋め学習UI
- 生成コードを「意味のある単位」に分解し役割ラベル・色・正解/誤答の選択肢を付与するチャンク化処理をAIProvider経由の追加リクエストとして実装
- CodeMirror 6のカスタム拡張として、正解コードのゴースト表示（低opacityテキスト）と、空欄部分のインタラクティブなブロックスロットを実装
- 画面下部/横にブロックパレット（役割ラベル・色付き）を実装し、ドラッグまたはタップでスロットに配置できるようにする
- 正しいブロックが配置されるたびに対象ファイルの内容を更新し、WebContainer上のファイルを更新してプレビューに即座に反映する
- **受け入れ基準**: 生成されたコードのうち最低1ファイルで、ブロックを正しく配置していくとプレビューの見た目が段階的に変化していく様子をブラウザで確認する

### Phase 4: 仕上げ
- 3ステップ全体を一気通貫で体験できるようにする（企画書入力→対話→技術選定確定→コード生成→ブロック学習→プレビュー）
- IndexedDBへの状態保存・リロード後の復元を実装
- 主要なローディング状態・エラーハンドリング（Ollama未起動/疎通エラー、モデル未pull、WebContainer起動失敗等）のUIを整備
- レスポンシブの最低限対応（PC優先、タブレット崩れ確認程度でよい）
- **受け入れ基準**: 初めてアプリを開いたユーザーが迷わず一連の流れを最後まで体験でき、リロードしても直前の状態から再開できることを確認する

---

## 5. 自走の心得

- 各フェーズの実装が終わったら、`npm run dev` 等でアプリを実際に起動し、ブラウザで機能を操作して動作確認すること。型チェックやビルドが通ることだけでは「機能が動く」ことの証明にならない。
- 判断に迷った場合はまず本ドキュメントの方針に立ち返ること。本ドキュメントに明記されていない実装の詳細（コンポーネント分割、命名、ファイル構成等）はあなた自身の裁量で妥当な設計を選んでよい。
- スコープ外セクションに該当する機能を「あると良さそう」という理由だけで追加しないこと。
- 意味のある単位でコミットを作成しながら進めること（フェーズ単位、または各フェーズ内の大きなマイルストーン単位）。
- 本当に確認が必要な場合（例：デフォルトで使うOllamaモデル名の選択、UIの見た目の好み等ユーザーの主観が強く影響する箇所）以外は、ユーザーに質問せず自分で決めて前進すること。
- **Anthropic API / Anthropic SDKは本プロジェクトのコードから一切呼び出さないこと。** AI呼び出しは必ず`AIProvider`インターフェース経由でローカルのOllamaに向けること。
